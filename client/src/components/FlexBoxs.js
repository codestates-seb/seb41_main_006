import styled from 'styled-components';
import {
  flexCenter,
  flexColCenter,
  flexRowCenter,
} from '../style/styleVariable';

export const CenterBox = styled.div`
  ${flexCenter}
`;
export const ColCenterBox = styled.div`
  ${flexColCenter}
`;
export const RowCenterBox = styled.div`
  ${flexRowCenter}
`;
